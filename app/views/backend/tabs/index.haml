- if Page.all.count > 0
  %p#notice= notice
  %h1 Список табов
  = link_to 'Добавить таб', new_backend_tab_path, class: 'btn btn-primary'
  %br
  %br


  %table.table.table-hover
    %thead
      %tr
        %th Заголовок
        %th На странице
        %th{:colspan => "3"}
    %tbody
      - @tabs.each do |tab|
        %tr
          %td= tab.title
          %td= tab.page.title
          %td= link_to 'Редактировать', edit_backend_tab_path(tab), class: 'btn btn-info btn-block btn-xs'
          %td= link_to 'Удалить', backend_tab_path(tab), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-block btn-xs'

- else

  Добавьте хотя бы одну страницу