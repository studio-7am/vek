= simple_form_for [:backend, @lombard_order] do |f|
  = f.input :name
  = f.input :phone
  %h3 Products
  #tasks
    = f.simple_fields_for :products do |product|
      = render 'product_fields', f: product
    .links
      = link_to_add_association 'add product', f, :products
  = f.submit